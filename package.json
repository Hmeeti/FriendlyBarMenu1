{
  "name": "friendly-menu",
  "version": "1.0.0",
  "private": true,
  "description": "Electronic restaurant menu with admin panel and realtime API",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix admin-web",
    "dev:api": "npm run dev --prefix backend",
    "dev:admin": "npm run dev --prefix admin-web",
    "start:api": "npm start --prefix backend",
    "db:push": "npm run db:push --prefix backend",
    "db:seed": "npm run db:seed --prefix backend",
    "setup": "npm run install:all && npm run db:push && npm run db:seed"
  },
  "engines": {
    "node": ">=20"
  }
}
