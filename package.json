{
  "name": "friendly-menu",
  "private": true,
  "scripts": {
    "dev:api": "npm run dev --prefix backend",
    "setup": "npm install --prefix backend && npm run db:push --prefix backend && npm run db:seed --prefix backend"
  }
}
